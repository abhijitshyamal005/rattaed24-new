import { Inter } from "next/font/google";

const inter = Inter({
	subsets: ["latin"],
	weight: ["200", "400", "500", "600", "700"],
	variable: "--font-inter",
	display: "swap",
});

export { inter };
